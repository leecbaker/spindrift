{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "signed release binary",
            "type": "shell",
            "command": "cargo build --release && codesign --force  --sign - --timestamp=none --entitlements debug.entitlements target/release/spindrift"
        }
    ]
}